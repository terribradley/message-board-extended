import { popularQuestion } from 'message-board/helpers/popular-question';
import { module, test } from 'qunit';

module('Unit | Helper | popular question');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = popularQuestion([42]);
  assert.ok(result);
});
